describe("NodeOperatorRegistry", () => {
    describe("usePubKeysForValidatorSetup", () => {
        it("correctly registers validator pubkeys");
        it("reverts if not called by the deposit manager");
        /** ... rest of reverts checks */
    });

    /** ... rest of external / public functions */
    describe("addNewValidatorDetails", () => {
        it("allows an operator to register a validator");
        /** ... rest of reverts checks */
    });
});
